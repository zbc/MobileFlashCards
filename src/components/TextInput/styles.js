import EStyleSheet from "react-native-extended-stylesheet";

export default EStyleSheet.create({
  container: {
    marginBottom: 20,
    padding: 10,
    marginHorizontal: 50,
    color: "$darkGray",
    borderRadius: 2,
    fontSize: 20,
    fontWeight: "600",
    borderWidth: 1
  }
});
